#include <G10/GXEPA.h>

const size_t faces[] = {
	0, 1, 2,
	0, 3, 1,
	0, 2, 3,
	1, 3, 2
};

void EPA(GXCollision_t* collision, vec3* simplex)
{

	vec3 min_normal;
	float min_distance = FLT_MAX;
	while (min_distance == FLT_MAX)
	{

	}
}
